[package]
name = "tallyowl-compat"
description = "Compatibility receivers: Prometheus and OpenMetrics scraping, and OpenTelemetry metric and trace push."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lints]
workspace = true

[dependencies]
tallyowl-obs = { workspace = true }
tallyowl-wire = { workspace = true }
tallyowl-collector-api = { workspace = true }
# An OpenTelemetry data point carries no event identifier, so this edge issues
# one. The random part must not be guessable, for the same reason a span ID
# must not be.
getrandom = { workspace = true }
