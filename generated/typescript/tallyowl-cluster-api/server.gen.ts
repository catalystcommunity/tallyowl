// Code generated by csilgen. DO NOT EDIT.
// Source: <csil spec>
// Target: typescript-server

import type { AssignProjectRequest, BootstrapGroupRequest, ChangeReplicaRequest, ClearDegradedRequest, ClusterAck, ClusterSnapshotRequest, ClusterSnapshotResponse, ConsensusMessage, ConsensusReply, DescribeTopologyRequest, DirectoryRequest, DirectoryResponse, FailOverRegionRequest, MergeTabletsRequest, MoveTabletRequest, NodeHealthReport, PartialQueryRequest, PartialQueryResponse, RemoveReplicaRequest, ReplicaStatus, ReplicaStatusRequest, RestoreRequest, RouteRequest, RouteResponse, SegmentList, SegmentListRequest, SegmentTransfer, SegmentTransferRequest, ServiceError, SetReceiptPolicyRequest, SnapshotChunk, SnapshotChunkRequest, SplitTabletRequest, TopologyResponse, UnsafeRecoverRequest, UnsafeRecoverResponse } from "./types.gen.ts";

// The consumer-defined context carries auth, request id, etc. It is opaque to
// generated code; the adapter defines its shape.
export interface RequestContext {
  [key: string]: unknown;
}

export interface Codec {
  decode<T>(bytes: Uint8Array): T;
  encode(value: unknown): Uint8Array;
}

export interface TallyOwlClusterHandlers {
  /**
   * The whole topology, or one cell's part of it.
   */
  describeTopology(req: DescribeTopologyRequest, ctx: RequestContext): Promise<TopologyResponse>;
  /**
   * Where a write for one project and affinity key belongs.
   */
  route(req: RouteRequest, ctx: RequestContext): Promise<RouteResponse>;
  /**
   * Create a group. The only operation that creates a voter set.
   */
  bootstrapGroup(req: BootstrapGroupRequest, ctx: RequestContext): Promise<ClusterAck>;
  /**
   * Add a replica, voting or not. Online.
   */
  addReplica(req: ChangeReplicaRequest, ctx: RequestContext): Promise<ClusterAck>;
  /**
   * Remove a replica. Online, and refused when it would end the quorum.
   */
  removeReplica(req: RemoveReplicaRequest, ctx: RequestContext): Promise<ClusterAck>;
  /**
   * Split one tablet into two at a virtual-shard boundary.
   */
  splitTablet(req: SplitTabletRequest, ctx: RequestContext): Promise<ClusterAck>;
  /**
   * Merge two adjacent tablets.
   */
  mergeTablets(req: MergeTabletsRequest, ctx: RequestContext): Promise<ClusterAck>;
  /**
   * Move a tablet from one node to another, online and with parity checked.
   */
  moveTablet(req: MoveTabletRequest, ctx: RequestContext): Promise<ClusterAck>;
  /**
   * Change a tablet's receipt policy. `local-one` is refused on a tablet
   * with more than one voter, and the refusal says so rather than quietly
   * selecting another policy.
   */
  setReceiptPolicy(req: SetReceiptPolicyRequest, ctx: RequestContext): Promise<ClusterAck>;
  /**
   * Move a tablet's write region behind a fence.
   */
  failOverRegion(req: FailOverRegionRequest, ctx: RequestContext): Promise<ClusterAck>;
  /**
   * Force a single-voter membership from a survivor. It can lose an
   * acknowledged write, it is audited, and it marks the range degraded.
   */
  unsafeRecover(req: UnsafeRecoverRequest, ctx: RequestContext): Promise<UnsafeRecoverResponse>;
  /**
   * Clear a degraded mark. It records who accepted the loss.
   */
  clearDegraded(req: ClearDegradedRequest, ctx: RequestContext): Promise<ClusterAck>;
  /**
   * Take a cluster snapshot.
   */
  snapshotCluster(req: ClusterSnapshotRequest, ctx: RequestContext): Promise<ClusterSnapshotResponse>;
  /**
   * Restore from a snapshot. The default answer to a permanent quorum loss.
   */
  restore(req: RestoreRequest, ctx: RequestContext): Promise<ClusterAck>;
  /**
   * The global directory: which cells hold a project.
   */
  directory(req: DirectoryRequest, ctx: RequestContext): Promise<DirectoryResponse>;
  /**
   * Assign a project to cells. Refused while the directory is unreachable,
   * and existing cells keep working through that refusal.
   */
  assignProject(req: AssignProjectRequest, ctx: RequestContext): Promise<ClusterAck>;
}

export const TallyOwlClusterWireIds = {
  service: 5,
  ops: {
    describeTopology: 0,
    route: 1,
    bootstrapGroup: 2,
    addReplica: 3,
    removeReplica: 4,
    splitTablet: 5,
    mergeTablets: 6,
    moveTablet: 7,
    setReceiptPolicy: 8,
    failOverRegion: 9,
    unsafeRecover: 10,
    clearDegraded: 11,
    snapshotCluster: 12,
    restore: 13,
    directory: 14,
    assignProject: 15,
  },
} as const;

export interface TallyOwlReplicationHandlers {
  /**
   * Hand one consensus message to the named group.
   */
  deliverConsensus(req: ConsensusMessage, ctx: RequestContext): Promise<ConsensusReply>;
  /**
   * Read part of a snapshot, so a new or lagging replica can catch up.
   */
  fetchSnapshotChunk(req: SnapshotChunkRequest, ctx: RequestContext): Promise<SnapshotChunk>;
  /**
   * Read part of a sealed segment, for a tablet that is moving.
   */
  fetchSegment(req: SegmentTransferRequest, ctx: RequestContext): Promise<SegmentTransfer>;
  /**
   * Compute a partial aggregate over one tablet. The coordinator merges.
   */
  partialAggregate(req: PartialQueryRequest, ctx: RequestContext): Promise<PartialQueryResponse>;
  /**
   * What this node holds, and how current each of it is.
   */
  replicaStatus(req: ReplicaStatusRequest, ctx: RequestContext): Promise<ReplicaStatus>;
  /**
   * A node reports its own latency and device readings. The controller
   * compares; the node never decides that it is slow.
   */
  reportHealth(req: NodeHealthReport, ctx: RequestContext): Promise<ClusterAck>;
  /**
   * Which sealed segments this node holds for one tablet.
   */
  listSegments(req: SegmentListRequest, ctx: RequestContext): Promise<SegmentList>;
}

export const TallyOwlReplicationWireIds = {
  service: 4,
  ops: {
    deliverConsensus: 0,
    fetchSnapshotChunk: 1,
    fetchSegment: 2,
    partialAggregate: 3,
    replicaStatus: 4,
    reportHealth: 5,
    listSegments: 6,
  },
} as const;

export interface ServerHandlers {
  tallyOwlCluster: TallyOwlClusterHandlers;
  tallyOwlReplication: TallyOwlReplicationHandlers;
}

/**
 * Dispatch a single call. The caller decodes service+method from the wire,
 * passes raw request bytes plus a codec, and receives raw response bytes.
 * Throws ServiceError for unknown service/method routes.
 */
export async function dispatch(
  handlers: ServerHandlers,
  codec: Codec,
  service: string,
  method: string,
  reqBytes: Uint8Array,
  ctx: RequestContext,
): Promise<Uint8Array> {
  switch (service) {
    case "TallyOwlCluster": {
      switch (method) {
        case "describe-topology": {
          const req = codec.decode<DescribeTopologyRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.describeTopology(req, ctx);
          return codec.encode(res);
        }
        case "route": {
          const req = codec.decode<RouteRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.route(req, ctx);
          return codec.encode(res);
        }
        case "bootstrap-group": {
          const req = codec.decode<BootstrapGroupRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.bootstrapGroup(req, ctx);
          return codec.encode(res);
        }
        case "add-replica": {
          const req = codec.decode<ChangeReplicaRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.addReplica(req, ctx);
          return codec.encode(res);
        }
        case "remove-replica": {
          const req = codec.decode<RemoveReplicaRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.removeReplica(req, ctx);
          return codec.encode(res);
        }
        case "split-tablet": {
          const req = codec.decode<SplitTabletRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.splitTablet(req, ctx);
          return codec.encode(res);
        }
        case "merge-tablets": {
          const req = codec.decode<MergeTabletsRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.mergeTablets(req, ctx);
          return codec.encode(res);
        }
        case "move-tablet": {
          const req = codec.decode<MoveTabletRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.moveTablet(req, ctx);
          return codec.encode(res);
        }
        case "set-receipt-policy": {
          const req = codec.decode<SetReceiptPolicyRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.setReceiptPolicy(req, ctx);
          return codec.encode(res);
        }
        case "fail-over-region": {
          const req = codec.decode<FailOverRegionRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.failOverRegion(req, ctx);
          return codec.encode(res);
        }
        case "unsafe-recover": {
          const req = codec.decode<UnsafeRecoverRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.unsafeRecover(req, ctx);
          return codec.encode(res);
        }
        case "clear-degraded": {
          const req = codec.decode<ClearDegradedRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.clearDegraded(req, ctx);
          return codec.encode(res);
        }
        case "snapshot-cluster": {
          const req = codec.decode<ClusterSnapshotRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.snapshotCluster(req, ctx);
          return codec.encode(res);
        }
        case "restore": {
          const req = codec.decode<RestoreRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.restore(req, ctx);
          return codec.encode(res);
        }
        case "directory": {
          const req = codec.decode<DirectoryRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.directory(req, ctx);
          return codec.encode(res);
        }
        case "assign-project": {
          const req = codec.decode<AssignProjectRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.assignProject(req, ctx);
          return codec.encode(res);
        }
        default:
          throw { code: 404, message: `unknown method ${service}.${method}` } satisfies ServiceError;
      }
    }
    case "TallyOwlReplication": {
      switch (method) {
        case "deliver-consensus": {
          const req = codec.decode<ConsensusMessage>(reqBytes);
          const res = await handlers.tallyOwlReplication.deliverConsensus(req, ctx);
          return codec.encode(res);
        }
        case "fetch-snapshot-chunk": {
          const req = codec.decode<SnapshotChunkRequest>(reqBytes);
          const res = await handlers.tallyOwlReplication.fetchSnapshotChunk(req, ctx);
          return codec.encode(res);
        }
        case "fetch-segment": {
          const req = codec.decode<SegmentTransferRequest>(reqBytes);
          const res = await handlers.tallyOwlReplication.fetchSegment(req, ctx);
          return codec.encode(res);
        }
        case "partial-aggregate": {
          const req = codec.decode<PartialQueryRequest>(reqBytes);
          const res = await handlers.tallyOwlReplication.partialAggregate(req, ctx);
          return codec.encode(res);
        }
        case "replica-status": {
          const req = codec.decode<ReplicaStatusRequest>(reqBytes);
          const res = await handlers.tallyOwlReplication.replicaStatus(req, ctx);
          return codec.encode(res);
        }
        case "report-health": {
          const req = codec.decode<NodeHealthReport>(reqBytes);
          const res = await handlers.tallyOwlReplication.reportHealth(req, ctx);
          return codec.encode(res);
        }
        case "list-segments": {
          const req = codec.decode<SegmentListRequest>(reqBytes);
          const res = await handlers.tallyOwlReplication.listSegments(req, ctx);
          return codec.encode(res);
        }
        default:
          throw { code: 404, message: `unknown method ${service}.${method}` } satisfies ServiceError;
      }
    }
    default:
      throw { code: 404, message: `unknown service ${service}` } satisfies ServiceError;
  }
}

/**
 * Compact-profile twin of dispatch: the caller decodes the service and
 * operation @wire-id ordinals from the wire and routes on those instead of
 * names. The host calls whichever twin matches the negotiated profile. The
 * rpc-mode poll fallback (<Op>Send/<Op>Check) has no single compact ordinal
 * and stays on the verbose `dispatch`.
 */
export async function dispatchCompact(
  handlers: ServerHandlers,
  codec: Codec,
  service: number,
  method: number,
  reqBytes: Uint8Array,
  ctx: RequestContext,
): Promise<Uint8Array> {
  switch (service) {
    case 5: {
      switch (method) {
        case 0: {
          const req = codec.decode<DescribeTopologyRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.describeTopology(req, ctx);
          return codec.encode(res);
        }
        case 1: {
          const req = codec.decode<RouteRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.route(req, ctx);
          return codec.encode(res);
        }
        case 2: {
          const req = codec.decode<BootstrapGroupRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.bootstrapGroup(req, ctx);
          return codec.encode(res);
        }
        case 3: {
          const req = codec.decode<ChangeReplicaRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.addReplica(req, ctx);
          return codec.encode(res);
        }
        case 4: {
          const req = codec.decode<RemoveReplicaRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.removeReplica(req, ctx);
          return codec.encode(res);
        }
        case 5: {
          const req = codec.decode<SplitTabletRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.splitTablet(req, ctx);
          return codec.encode(res);
        }
        case 6: {
          const req = codec.decode<MergeTabletsRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.mergeTablets(req, ctx);
          return codec.encode(res);
        }
        case 7: {
          const req = codec.decode<MoveTabletRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.moveTablet(req, ctx);
          return codec.encode(res);
        }
        case 8: {
          const req = codec.decode<SetReceiptPolicyRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.setReceiptPolicy(req, ctx);
          return codec.encode(res);
        }
        case 9: {
          const req = codec.decode<FailOverRegionRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.failOverRegion(req, ctx);
          return codec.encode(res);
        }
        case 10: {
          const req = codec.decode<UnsafeRecoverRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.unsafeRecover(req, ctx);
          return codec.encode(res);
        }
        case 11: {
          const req = codec.decode<ClearDegradedRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.clearDegraded(req, ctx);
          return codec.encode(res);
        }
        case 12: {
          const req = codec.decode<ClusterSnapshotRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.snapshotCluster(req, ctx);
          return codec.encode(res);
        }
        case 13: {
          const req = codec.decode<RestoreRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.restore(req, ctx);
          return codec.encode(res);
        }
        case 14: {
          const req = codec.decode<DirectoryRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.directory(req, ctx);
          return codec.encode(res);
        }
        case 15: {
          const req = codec.decode<AssignProjectRequest>(reqBytes);
          const res = await handlers.tallyOwlCluster.assignProject(req, ctx);
          return codec.encode(res);
        }
        default:
          throw { code: 404, message: `unknown ordinal ${service}.${method}` } satisfies ServiceError;
      }
    }
    case 4: {
      switch (method) {
        case 0: {
          const req = codec.decode<ConsensusMessage>(reqBytes);
          const res = await handlers.tallyOwlReplication.deliverConsensus(req, ctx);
          return codec.encode(res);
        }
        case 1: {
          const req = codec.decode<SnapshotChunkRequest>(reqBytes);
          const res = await handlers.tallyOwlReplication.fetchSnapshotChunk(req, ctx);
          return codec.encode(res);
        }
        case 2: {
          const req = codec.decode<SegmentTransferRequest>(reqBytes);
          const res = await handlers.tallyOwlReplication.fetchSegment(req, ctx);
          return codec.encode(res);
        }
        case 3: {
          const req = codec.decode<PartialQueryRequest>(reqBytes);
          const res = await handlers.tallyOwlReplication.partialAggregate(req, ctx);
          return codec.encode(res);
        }
        case 4: {
          const req = codec.decode<ReplicaStatusRequest>(reqBytes);
          const res = await handlers.tallyOwlReplication.replicaStatus(req, ctx);
          return codec.encode(res);
        }
        case 5: {
          const req = codec.decode<NodeHealthReport>(reqBytes);
          const res = await handlers.tallyOwlReplication.reportHealth(req, ctx);
          return codec.encode(res);
        }
        case 6: {
          const req = codec.decode<SegmentListRequest>(reqBytes);
          const res = await handlers.tallyOwlReplication.listSegments(req, ctx);
          return codec.encode(res);
        }
        default:
          throw { code: 404, message: `unknown ordinal ${service}.${method}` } satisfies ServiceError;
      }
    }
    default:
      throw { code: 404, message: `unknown service ordinal ${service}` } satisfies ServiceError;
  }
}
