// Code generated by csilgen. DO NOT EDIT.
// Source: <csil spec>
// Target: typescript-server

import type { AlertInstance, AlertInstanceList, AlertListRequest, AlertRule, AlertRuleList, ApiKeyList, AttributionSettings, BeginLoginRequest, BeginLoginResponse, CompiledPolicy, CompleteLoginRequest, CompleteLoginResponse, CreateRoleTokenRequest, CreateRoleTokenResponse, DeleteAlertRuleRequest, DeletionRequest, DeletionResponse, Empty, EnrollNodeRequest, EnrollNodeResponse, ListRequest, NodeList, NotificationDeliveryList, PolicyDocument, PolicyRequest, ProjectList, QueryRequest, QueryResponse, RenewNodeCertificateRequest, ResolveRequest, RevokeRoleTokenRequest, RoleTokenList, RunWorkflowRequest, RunWorkflowResponse, SavedAnalysis, SavedAnalysisList, SavedDashboard, SavedDashboardList, SavedRequest, ServiceError, SilenceRequest, WorkflowList, WorkspaceList } from "./types.gen.ts";

// The consumer-defined context carries auth, request id, etc. It is opaque to
// generated code; the adapter defines its shape.
export interface RequestContext {
  [key: string]: unknown;
}

export interface Codec {
  decode<T>(bytes: Uint8Array): T;
  encode(value: unknown): Uint8Array;
}

export interface TallyOwlControlHandlers {
  /**
   * Run one analytic query.
   */
  runQuery(req: QueryRequest, ctx: RequestContext): Promise<QueryResponse>;
  /**
   * List the workspaces that the session can read.
   */
  listWorkspaces(req: ListRequest, ctx: RequestContext): Promise<WorkspaceList>;
  /**
   * List the projects that the session can read.
   */
  listProjects(req: ListRequest, ctx: RequestContext): Promise<ProjectList>;
  /**
   * List the active keys for a project.
   */
  listApiKeys(req: ListRequest, ctx: RequestContext): Promise<ApiKeyList>;
  /**
   * Create or replace an alert rule.
   */
  putAlertRule(req: AlertRule, ctx: RequestContext): Promise<AlertRule>;
  /**
   * List alert rules.
   */
  listAlertRules(req: AlertListRequest, ctx: RequestContext): Promise<AlertRuleList>;
  /**
   * List current alert instances and their state.
   */
  listAlertInstances(req: AlertListRequest, ctx: RequestContext): Promise<AlertInstanceList>;
  /**
   * Start an erasure. The tombstone applies immediately and stays active
   * for late arrivals until its horizon ends. See D28.
   */
  requestDeletion(req: DeletionRequest, ctx: RequestContext): Promise<DeletionResponse>;
  /**
   * Fetch the compiled collection policy for a source.
   */
  getPolicy(req: PolicyRequest, ctx: RequestContext): Promise<CompiledPolicy>;
  /**
   * Start a sign-in. Returns the URL to send the browser to.
   */
  beginLogin(req: BeginLoginRequest, ctx: RequestContext): Promise<BeginLoginResponse>;
  /**
   * Finish a sign-in with what the callback carried, and get a session.
   */
  completeLogin(req: CompleteLoginRequest, ctx: RequestContext): Promise<CompleteLoginResponse>;
  /**
   * Create a reusable role token. The token text is returned once and is
   * never recoverable afterwards.
   */
  createRoleToken(req: CreateRoleTokenRequest, ctx: RequestContext): Promise<CreateRoleTokenResponse>;
  /**
   * List role tokens and what each one has enrolled.
   */
  listRoleTokens(req: ListRequest, ctx: RequestContext): Promise<RoleTokenList>;
  /**
   * Stop a token enrolling anything more.
   */
  revokeRoleToken(req: RevokeRoleTokenRequest, ctx: RequestContext): Promise<Empty>;
  /**
   * Enroll a node. The node generates its private key and sends only a
   * certificate request; the control plane signs and never sees the key.
   */
  enrollNode(req: EnrollNodeRequest, ctx: RequestContext): Promise<EnrollNodeResponse>;
  /**
   * Renew a node certificate against the node's current identity.
   */
  renewNodeCertificate(req: RenewNodeCertificateRequest, ctx: RequestContext): Promise<EnrollNodeResponse>;
  /**
   * List enrolled nodes.
   */
  listNodes(req: ListRequest, ctx: RequestContext): Promise<NodeList>;
  /**
   * Set one level of collection policy. It compiles before it is stored,
   * because invalid policy never replaces valid policy.
   */
  putPolicy(req: PolicyDocument, ctx: RequestContext): Promise<CompiledPolicy>;
  /**
   * Save an analysis, or replace it.
   */
  putAnalysis(req: SavedAnalysis, ctx: RequestContext): Promise<SavedAnalysis>;
  /**
   * List the saved analyses of a project.
   */
  listAnalyses(req: SavedRequest, ctx: RequestContext): Promise<SavedAnalysisList>;
  /**
   * Remove a saved analysis. Refused while a dashboard shows it.
   */
  deleteAnalysis(req: SavedRequest, ctx: RequestContext): Promise<Empty>;
  /**
   * Save a dashboard, or replace it.
   */
  putDashboard(req: SavedDashboard, ctx: RequestContext): Promise<SavedDashboard>;
  /**
   * List the dashboards of a project.
   */
  listDashboards(req: SavedRequest, ctx: RequestContext): Promise<SavedDashboardList>;
  /**
   * Set the attribution weights and windows of a project. A change
   * recomputes every later result and rewrites no stored fact. See D40.
   */
  putAttributionSettings(req: AttributionSettings, ctx: RequestContext): Promise<AttributionSettings>;
  /**
   * Read the attribution settings a project answers under.
   */
  getAttributionSettings(req: SavedRequest, ctx: RequestContext): Promise<AttributionSettings>;
  /**
   * Remove a dashboard. The analyses it showed stay, because an analysis is
   * a question somebody saved and a dashboard is only one arrangement of
   * several. Removing an analysis is `delete-analysis`, and it is refused
   * while a dashboard still shows it.
   */
  deleteDashboard(req: SavedRequest, ctx: RequestContext): Promise<Empty>;
  /**
   * Remove an alert rule and its instance.
   */
  deleteAlertRule(req: DeleteAlertRuleRequest, ctx: RequestContext): Promise<Empty>;
  /**
   * Suppress notification for a period. The evaluation still runs and still
   * records state, so an operator can see what happened while it was quiet.
   */
  silenceAlert(req: SilenceRequest, ctx: RequestContext): Promise<AlertInstance>;
  /**
   * Put a firing rule back to `ok` by hand and send the resolution.
   */
  resolveAlert(req: ResolveRequest, ctx: RequestContext): Promise<AlertInstance>;
  /**
   * What every workflow is doing: its lag, its failures, and what is in
   * quarantine waiting for a person.
   */
  listWorkflows(req: ListRequest, ctx: RequestContext): Promise<WorkflowList>;
  /**
   * Start one workflow now rather than at its schedule.
   */
  runWorkflow(req: RunWorkflowRequest, ctx: RequestContext): Promise<RunWorkflowResponse>;
  /**
   * Every notification attempt, delivered or not.
   */
  listNotifications(req: AlertListRequest, ctx: RequestContext): Promise<NotificationDeliveryList>;
}

export const TallyOwlControlWireIds = {
  service: 3,
  ops: {
    runQuery: 0,
    listWorkspaces: 1,
    listProjects: 2,
    listApiKeys: 3,
    putAlertRule: 4,
    listAlertRules: 5,
    listAlertInstances: 6,
    requestDeletion: 7,
    getPolicy: 8,
    beginLogin: 9,
    completeLogin: 10,
    createRoleToken: 11,
    listRoleTokens: 12,
    revokeRoleToken: 13,
    enrollNode: 14,
    renewNodeCertificate: 15,
    listNodes: 16,
    putPolicy: 17,
    putAnalysis: 18,
    listAnalyses: 19,
    deleteAnalysis: 20,
    putDashboard: 21,
    listDashboards: 22,
    putAttributionSettings: 23,
    getAttributionSettings: 24,
    deleteDashboard: 25,
    deleteAlertRule: 26,
    silenceAlert: 27,
    resolveAlert: 28,
    listWorkflows: 29,
    runWorkflow: 30,
    listNotifications: 31,
  },
} as const;

export interface ServerHandlers {
  tallyOwlControl: TallyOwlControlHandlers;
}

/**
 * Dispatch a single call. The caller decodes service+method from the wire,
 * passes raw request bytes plus a codec, and receives raw response bytes.
 * Throws ServiceError for unknown service/method routes.
 */
export async function dispatch(
  handlers: ServerHandlers,
  codec: Codec,
  service: string,
  method: string,
  reqBytes: Uint8Array,
  ctx: RequestContext,
): Promise<Uint8Array> {
  switch (service) {
    case "TallyOwlControl": {
      switch (method) {
        case "run-query": {
          const req = codec.decode<QueryRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.runQuery(req, ctx);
          return codec.encode(res);
        }
        case "list-workspaces": {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listWorkspaces(req, ctx);
          return codec.encode(res);
        }
        case "list-projects": {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listProjects(req, ctx);
          return codec.encode(res);
        }
        case "list-api-keys": {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listApiKeys(req, ctx);
          return codec.encode(res);
        }
        case "put-alert-rule": {
          const req = codec.decode<AlertRule>(reqBytes);
          const res = await handlers.tallyOwlControl.putAlertRule(req, ctx);
          return codec.encode(res);
        }
        case "list-alert-rules": {
          const req = codec.decode<AlertListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listAlertRules(req, ctx);
          return codec.encode(res);
        }
        case "list-alert-instances": {
          const req = codec.decode<AlertListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listAlertInstances(req, ctx);
          return codec.encode(res);
        }
        case "request-deletion": {
          const req = codec.decode<DeletionRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.requestDeletion(req, ctx);
          return codec.encode(res);
        }
        case "get-policy": {
          const req = codec.decode<PolicyRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.getPolicy(req, ctx);
          return codec.encode(res);
        }
        case "begin-login": {
          const req = codec.decode<BeginLoginRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.beginLogin(req, ctx);
          return codec.encode(res);
        }
        case "complete-login": {
          const req = codec.decode<CompleteLoginRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.completeLogin(req, ctx);
          return codec.encode(res);
        }
        case "create-role-token": {
          const req = codec.decode<CreateRoleTokenRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.createRoleToken(req, ctx);
          return codec.encode(res);
        }
        case "list-role-tokens": {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listRoleTokens(req, ctx);
          return codec.encode(res);
        }
        case "revoke-role-token": {
          const req = codec.decode<RevokeRoleTokenRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.revokeRoleToken(req, ctx);
          return codec.encode(res);
        }
        case "enroll-node": {
          const req = codec.decode<EnrollNodeRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.enrollNode(req, ctx);
          return codec.encode(res);
        }
        case "renew-node-certificate": {
          const req = codec.decode<RenewNodeCertificateRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.renewNodeCertificate(req, ctx);
          return codec.encode(res);
        }
        case "list-nodes": {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listNodes(req, ctx);
          return codec.encode(res);
        }
        case "put-policy": {
          const req = codec.decode<PolicyDocument>(reqBytes);
          const res = await handlers.tallyOwlControl.putPolicy(req, ctx);
          return codec.encode(res);
        }
        case "put-analysis": {
          const req = codec.decode<SavedAnalysis>(reqBytes);
          const res = await handlers.tallyOwlControl.putAnalysis(req, ctx);
          return codec.encode(res);
        }
        case "list-analyses": {
          const req = codec.decode<SavedRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listAnalyses(req, ctx);
          return codec.encode(res);
        }
        case "delete-analysis": {
          const req = codec.decode<SavedRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.deleteAnalysis(req, ctx);
          return codec.encode(res);
        }
        case "put-dashboard": {
          const req = codec.decode<SavedDashboard>(reqBytes);
          const res = await handlers.tallyOwlControl.putDashboard(req, ctx);
          return codec.encode(res);
        }
        case "list-dashboards": {
          const req = codec.decode<SavedRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listDashboards(req, ctx);
          return codec.encode(res);
        }
        case "put-attribution-settings": {
          const req = codec.decode<AttributionSettings>(reqBytes);
          const res = await handlers.tallyOwlControl.putAttributionSettings(req, ctx);
          return codec.encode(res);
        }
        case "get-attribution-settings": {
          const req = codec.decode<SavedRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.getAttributionSettings(req, ctx);
          return codec.encode(res);
        }
        case "delete-dashboard": {
          const req = codec.decode<SavedRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.deleteDashboard(req, ctx);
          return codec.encode(res);
        }
        case "delete-alert-rule": {
          const req = codec.decode<DeleteAlertRuleRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.deleteAlertRule(req, ctx);
          return codec.encode(res);
        }
        case "silence-alert": {
          const req = codec.decode<SilenceRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.silenceAlert(req, ctx);
          return codec.encode(res);
        }
        case "resolve-alert": {
          const req = codec.decode<ResolveRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.resolveAlert(req, ctx);
          return codec.encode(res);
        }
        case "list-workflows": {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listWorkflows(req, ctx);
          return codec.encode(res);
        }
        case "run-workflow": {
          const req = codec.decode<RunWorkflowRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.runWorkflow(req, ctx);
          return codec.encode(res);
        }
        case "list-notifications": {
          const req = codec.decode<AlertListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listNotifications(req, ctx);
          return codec.encode(res);
        }
        default:
          throw { code: 404, message: `unknown method ${service}.${method}` } satisfies ServiceError;
      }
    }
    default:
      throw { code: 404, message: `unknown service ${service}` } satisfies ServiceError;
  }
}

/**
 * Compact-profile twin of dispatch: the caller decodes the service and
 * operation @wire-id ordinals from the wire and routes on those instead of
 * names. The host calls whichever twin matches the negotiated profile. The
 * rpc-mode poll fallback (<Op>Send/<Op>Check) has no single compact ordinal
 * and stays on the verbose `dispatch`.
 */
export async function dispatchCompact(
  handlers: ServerHandlers,
  codec: Codec,
  service: number,
  method: number,
  reqBytes: Uint8Array,
  ctx: RequestContext,
): Promise<Uint8Array> {
  switch (service) {
    case 3: {
      switch (method) {
        case 0: {
          const req = codec.decode<QueryRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.runQuery(req, ctx);
          return codec.encode(res);
        }
        case 1: {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listWorkspaces(req, ctx);
          return codec.encode(res);
        }
        case 2: {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listProjects(req, ctx);
          return codec.encode(res);
        }
        case 3: {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listApiKeys(req, ctx);
          return codec.encode(res);
        }
        case 4: {
          const req = codec.decode<AlertRule>(reqBytes);
          const res = await handlers.tallyOwlControl.putAlertRule(req, ctx);
          return codec.encode(res);
        }
        case 5: {
          const req = codec.decode<AlertListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listAlertRules(req, ctx);
          return codec.encode(res);
        }
        case 6: {
          const req = codec.decode<AlertListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listAlertInstances(req, ctx);
          return codec.encode(res);
        }
        case 7: {
          const req = codec.decode<DeletionRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.requestDeletion(req, ctx);
          return codec.encode(res);
        }
        case 8: {
          const req = codec.decode<PolicyRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.getPolicy(req, ctx);
          return codec.encode(res);
        }
        case 9: {
          const req = codec.decode<BeginLoginRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.beginLogin(req, ctx);
          return codec.encode(res);
        }
        case 10: {
          const req = codec.decode<CompleteLoginRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.completeLogin(req, ctx);
          return codec.encode(res);
        }
        case 11: {
          const req = codec.decode<CreateRoleTokenRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.createRoleToken(req, ctx);
          return codec.encode(res);
        }
        case 12: {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listRoleTokens(req, ctx);
          return codec.encode(res);
        }
        case 13: {
          const req = codec.decode<RevokeRoleTokenRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.revokeRoleToken(req, ctx);
          return codec.encode(res);
        }
        case 14: {
          const req = codec.decode<EnrollNodeRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.enrollNode(req, ctx);
          return codec.encode(res);
        }
        case 15: {
          const req = codec.decode<RenewNodeCertificateRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.renewNodeCertificate(req, ctx);
          return codec.encode(res);
        }
        case 16: {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listNodes(req, ctx);
          return codec.encode(res);
        }
        case 17: {
          const req = codec.decode<PolicyDocument>(reqBytes);
          const res = await handlers.tallyOwlControl.putPolicy(req, ctx);
          return codec.encode(res);
        }
        case 18: {
          const req = codec.decode<SavedAnalysis>(reqBytes);
          const res = await handlers.tallyOwlControl.putAnalysis(req, ctx);
          return codec.encode(res);
        }
        case 19: {
          const req = codec.decode<SavedRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listAnalyses(req, ctx);
          return codec.encode(res);
        }
        case 20: {
          const req = codec.decode<SavedRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.deleteAnalysis(req, ctx);
          return codec.encode(res);
        }
        case 21: {
          const req = codec.decode<SavedDashboard>(reqBytes);
          const res = await handlers.tallyOwlControl.putDashboard(req, ctx);
          return codec.encode(res);
        }
        case 22: {
          const req = codec.decode<SavedRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listDashboards(req, ctx);
          return codec.encode(res);
        }
        case 23: {
          const req = codec.decode<AttributionSettings>(reqBytes);
          const res = await handlers.tallyOwlControl.putAttributionSettings(req, ctx);
          return codec.encode(res);
        }
        case 24: {
          const req = codec.decode<SavedRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.getAttributionSettings(req, ctx);
          return codec.encode(res);
        }
        case 25: {
          const req = codec.decode<SavedRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.deleteDashboard(req, ctx);
          return codec.encode(res);
        }
        case 26: {
          const req = codec.decode<DeleteAlertRuleRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.deleteAlertRule(req, ctx);
          return codec.encode(res);
        }
        case 27: {
          const req = codec.decode<SilenceRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.silenceAlert(req, ctx);
          return codec.encode(res);
        }
        case 28: {
          const req = codec.decode<ResolveRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.resolveAlert(req, ctx);
          return codec.encode(res);
        }
        case 29: {
          const req = codec.decode<ListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listWorkflows(req, ctx);
          return codec.encode(res);
        }
        case 30: {
          const req = codec.decode<RunWorkflowRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.runWorkflow(req, ctx);
          return codec.encode(res);
        }
        case 31: {
          const req = codec.decode<AlertListRequest>(reqBytes);
          const res = await handlers.tallyOwlControl.listNotifications(req, ctx);
          return codec.encode(res);
        }
        default:
          throw { code: 404, message: `unknown ordinal ${service}.${method}` } satisfies ServiceError;
      }
    }
    default:
      throw { code: 404, message: `unknown service ordinal ${service}` } satisfies ServiceError;
  }
}
