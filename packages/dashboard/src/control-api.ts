// The generated control package, reached by path.
//
// The generated packages are checked in and live in this repository, so they
// resolve by path rather than through a registry. One module holds that path,
// so a move costs one edit rather than one for each import.
export * from "../../../generated/typescript/tallyowl-control-api/types.gen.ts";
export * from "../../../generated/typescript/tallyowl-control-api/codec.gen.ts";
