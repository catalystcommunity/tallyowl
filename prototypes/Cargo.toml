[workspace]
resolver = "2"
members = [
    "page-bench",
    "catalog-bench",
    "index-bench",
    "tier-bench",
    "consensus-bench",
    "wal-bench",
    "segment-bench",
    "locator-bench",
]

[workspace.package]
edition = "2021"
license = "Apache-2.0"
publish = false

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
