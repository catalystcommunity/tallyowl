[package]
name = "tier-bench"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
zstd = "0.13"
opendal = { version = "0.55", default-features = false, features = ["services-fs"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "fs", "time"] }
